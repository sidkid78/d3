export async function verifySubscription() {
    // Mock verification for Supabase bypass

    // Return a mock user
    const user = {
        id: 'mock-uuid-1234',
        email: 'agent@example.com'
    };

    return user;
}
